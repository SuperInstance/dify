from setuptools import setup, find_packages

setup(
    name="@superinstance/guardian",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.10",
    description="Workflow Conservation Engine — analyze Dify workflows for cost efficiency and detect waste.",
    author="SuperInstance",
)
